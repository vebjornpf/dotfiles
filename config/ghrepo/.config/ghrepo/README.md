# ghrepo

Supported commands:
- `ghrepo sync`

Local config:
- `~/.config/local/ghrepo.zsh`

Variables:
- `GHREPO_ORG` - required GitHub organization to sync

State files:
- `repos.tsv` - `<name>\t<url>`

Example:

```zsh
export GHREPO_ORG="elhub"
```
