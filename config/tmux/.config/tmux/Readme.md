Requires the Tmux Plugin Manager to work properly: https://github.com/tmux-plugins/tpm

Some issues I have encountered
- After starting tmux I have to source the tmux.conf first time?

Keybindings
- prefix + I (from TPM): install/sync all plugins listed in tmux.conf after adding/removing plugins.
- prefix + R: reload ~/.config/tmux/tmux.conf and show a “Config reloaded…” message without restarting tmux.
